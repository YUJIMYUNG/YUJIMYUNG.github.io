import React from 'react';

const Title = ({title}) => {
    return (
        <p className='text-5xl font-bold text-default tracking-tight sm:text-5xl'>
            {title} 
        </p>
    );
};

export default Title;