import React from 'react';

const Subtitle = ({title}) => {
    return (
        <h2 className='text-3xl font-bold text-default tracking-wide'>
            {title}
        </h2>
    );
};

export default Subtitle;